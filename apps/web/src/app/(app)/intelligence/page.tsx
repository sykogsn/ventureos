import { redirect } from "next/navigation";

/** Intelligence is the Situation Room, Company HQ, and Executive Office — not a fourth surface. */
export default function IntelligencePage() {
  redirect("/dashboard");
}
